[
  {
    "step_index": 385,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Updated first sales returns creation endpoint calculations with reverse-GST logic",
      "EndLine": 1424,
      "Instruction": "Update the sales return calculation logic to use reverse-GST formula in the first route handler",
      "ReplacementContent": "        const total = item.qty * item.rate;\n        const gstPercent = item.gstPercentage || 18;\n        const taxable = total / (1 + (gstPercent / 100));\n        const gstAmount = total - taxable;\n        \n        totalTaxable += taxable;\n        totalCGST += gstAmount / 2;\n        totalSGST += gstAmount / 2;\n      }\n\n      const grandTotal = totalTaxable + totalCGST + totalSGST;\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: item.qty * item.rate,\n        distributorId,\n        csaId\n      }));",
      "StartLine": 1405,
      "TargetContent": "        const taxable = item.qty * item.rate;\n        totalTaxable += taxable;\n        const gstAmount = (taxable * item.gstPercentage) / 100;\n        totalCGST += gstAmount / 2;\n        totalSGST += gstAmount / 2;\n      }\n\n      const grandTotal = totalTaxable + totalCGST + totalSGST;\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: (item.qty * item.rate) + ((item.qty * item.rate * item.gstPercentage) / 100),\n        distributorId,\n        csaId\n      }));",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js"
    }
  },
  {
    "step_index": 389,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Updated second sales returns creation route calculations with reverse-GST logic",
      "EndLine": 4570,
      "Instruction": "Update the sales return calculation logic to use reverse-GST formula in the second route handler",
      "ReplacementContent": "      for (const item of items) {\n        const total = item.qty * item.rate\n        const gstPercent = item.gstPercentage || 18\n        const taxable = total / (1 + (gstPercent / 100))\n        const gstAmount = total - taxable\n        \n        totalTaxable += taxable\n        totalCGST += gstAmount / 2\n        totalSGST += gstAmount / 2\n      }\n\n      const grandTotal = totalTaxable + totalCGST + totalSGST\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        distributorId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: item.qty * item.rate,\n        csaId\n      }))",
      "StartLine": 4548,
      "TargetContent": "      for (const item of items) {\n        const taxable = item.qty * item.rate\n        totalTaxable += taxable\n        // Calculate GST for display\n        const gstAmount = (taxable * item.gstPercentage) / 100\n        totalCGST += gstAmount / 2\n        totalSGST += gstAmount / 2\n      }\n\n      // Grand total equals taxable value since GST is already included in rate\n      const grandTotal = totalTaxable\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        distributorId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: item.qty * item.rate,\n        csaId\n      }))",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js"
    }
  },
  {
    "step_index": 393,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Updated third sales returns creation endpoint calculations with reverse-GST logic",
      "EndLine": 4718,
      "Instruction": "Update the sales return calculation logic to use reverse-GST formula in the third route handler",
      "ReplacementContent": "      for (const item of items) {\n        const total = item.qty * item.rate\n        const gstPercent = item.gstPercentage || 18\n        const taxable = total / (1 + (gstPercent / 100))\n        const gstAmount = total - taxable\n        \n        totalTaxable += taxable\n        totalCGST += gstAmount / 2\n        totalSGST += gstAmount / 2\n      }\n\n      const grandTotal = totalTaxable + totalCGST + totalSGST\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: item.qty * item.rate,\n        csaId\n      }))",
      "StartLine": 4699,
      "TargetContent": "      for (const item of items) {\n        const taxable = item.qty * item.rate\n        totalTaxable += taxable\n        const gstAmount = (taxable * item.gstPercentage) / 100\n        totalCGST += gstAmount / 2\n        totalSGST += gstAmount / 2\n      }\n\n      const grandTotal = totalTaxable + totalCGST + totalSGST\n\n      const salesReturnItemsData = items.map((item) => ({\n        productId: item.productId,\n        qty: item.qty,\n        costPrice: item.costPrice,\n        rate: item.rate,\n        gstPercentage: item.gstPercentage,\n        total: (item.qty * item.rate) + ((item.qty * item.rate * item.gstPercentage) / 100),\n        csaId\n      }))",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js"
    }
  },
  {
    "step_index": 622,
    "name": "multi_replace_file_content",
    "args": {
      "Description": "Auto-generate distributor purchases and sync product stock when CSA creates, updates, or deletes invoices.",
      "Instruction": "Modify POST /distributors/:distributorId/invoices/create, PUT /invoices/:id, and DELETE /invoices/:id to create, update, and delete matching PurchaseLedger/PurchaseItem records and sync distributor product stock.",
      "ReplacementChunks": [
        {
          "AllowMultiple": false,
          "EndLine": 936,
          "ReplacementContent": "router.post('/distributors/:distributorId/invoices/create', authenticateToken, requireCSA, async (req, res) => {\n  try {\n    const { distributorId } = req.params\n    const csaId = req.user.userId\n    const { partyId, items, isInterState } = req.body\n\n    const csaUser = await prisma.user.findUnique({\n      where: { id: csaId }\n    })\n    const csaName = csaUser ? csaUser.name : 'CSA'\n\n    console.log('Creating invoice:', { distributorId, csaId, partyId, items: items.length, isInterState })\n",
          "StartLine": 930,
          "TargetContent": "router.post('/distributors/:distributorId/invoices/create', authenticateToken, requireCSA, async (req, res) => {\n  try {\n    const { distributorId } = req.params\n    const csaId = req.user.userId\n    const { partyId, items, isInterState } = req.body\n\n    console.log('Creating invoice:', { distributorId, csaId, partyId, items: items.length, isInterState })\n"
        },
        {
          "AllowMultiple": false,
          "EndLine": 1081,
          "ReplacementContent": "      await tx.distributor.update({\n        where: { id: distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      // --- AUTO-GENERATE DISTRIBUTOR PURCHASE ---\n      // 1. Create PurchaseLedger\n      const purchaseLedger = await tx.purchaseLedger.create({\n        data: {\n          supplierName: csaName,\n          invoiceNo: nextInvoiceNo,\n          date: new Date(),\n          totalAmount: grandTotal,\n          distributorId: distributorId,\n          csaId: csaId\n        }\n      })\n\n      // 2. Loop through each created invoice item to update/create distributor products and create purchase items\n      for (let i = 0; i < invoiceItemsData.length; i++) {\n        const item = invoiceItemsData[i]\n        const csaProduct = productsData[i].product\n        \n        const rateWithMargin = item.total / item.qty\n\n        let distProduct = await tx.product.findFirst({\n          where: {\n            distributorId: distributorId,\n            sku: csaProduct.sku\n          }\n        })\n        if (!distProduct || !csaProduct.sku) {\n          distProduct = await tx.product.findFirst({\n            where: {\n              distributorId: distributorId,\n              name: csaProduct.name\n            }\n          })\n        }\n\n        if (distProduct) {\n          distProduct = await tx.product.update({\n            where: { id: distProduct.id },\n            data: {\n              currentStock: { increment: item.qty },\n              costPrice: rateWithMargin\n            }\n          })\n        } else {\n          distProduct = await tx.product.create({\n            data: {\n              distributorId: distributorId,\n              name: csaProduct.name,\n              sku: csaProduct.sku || `SKU-${Date.now()}-${i}`,\n              hsn: csaProduct.hsn,\n              batchNo: csaProduct.batchNo,\n              expiryDate: csaProduct.expiryDate,\n              costPrice: rateWithMargin,\n              baseSellingPrice: csaProduct.baseSellingPrice,\n              gstPercentage: csaProduct.gstPercentage,\n              currentStock: item.qty\n            }\n          })\n        }\n\n        await tx.purchaseItem.create({\n          data: {\n            purchaseId: purchaseLedger.id,\n            productId: distProduct.id,\n            distributorId: distributorId,\n            csaId: csaId,\n            qty: item.qty,\n            costPrice: rateWithMargin,\n            rate: rateWithMargin,\n            gstPercentage: item.gstPercentage,\n            total: item.total,\n            mrp: distProduct.baseSellingPrice,\n            batchNo: csaProduct.batchNo,\n            expiryDate: csaProduct.expiryDate\n          }\n        })\n      }\n\n      return {\n        ...invoice,\n        invoiceStockCost,\n        invoiceProfit,\n        profitStatus\n      }\n    })\n",
          "StartLine": 1067,
          "TargetContent": "      await tx.distributor.update({\n        where: { id: distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      return {\n        ...invoice,\n        invoiceStockCost,\n        invoiceProfit,\n        profitStatus\n      }\n    })\n"
        },
        {
          "AllowMultiple": false,
          "EndLine": 1121,
          "ReplacementContent": "router.put('/invoices/:id', authenticateToken, requireCSA, async (req, res) => {\n  try {\n    const { id } = req.params\n    const { items, isInterState } = req.body\n    const csaId = req.user.userId\n\n    const csaUser = await prisma.user.findUnique({\n      where: { id: csaId }\n    })\n    const csaName = csaUser ? csaUser.name : 'CSA'\n",
          "StartLine": 1116,
          "TargetContent": "router.put('/invoices/:id', authenticateToken, requireCSA, async (req, res) => {\n  try {\n    const { id } = req.params\n    const { items, isInterState } = req.body\n    const csaId = req.user.userId\n"
        },
        {
          "AllowMultiple": false,
          "EndLine": 1154,
          "ReplacementContent": "    const result = await prisma.$transaction(async (tx) => {\n      // Revert old distributor stock and delete old PurchaseLedger\n      const oldPurchaseLedger = await tx.purchaseLedger.findFirst({\n        where: {\n          invoiceNo: existingInvoice.invoiceNo,\n          distributorId: existingInvoice.distributorId\n        },\n        include: { purchaseItems: true }\n      })\n      if (oldPurchaseLedger) {\n        for (const pItem of oldPurchaseLedger.purchaseItems) {\n          await tx.product.update({\n            where: { id: pItem.productId },\n            data: { currentStock: { decrement: pItem.qty } }\n          })\n        }\n        await tx.purchaseItem.deleteMany({\n          where: { purchaseId: oldPurchaseLedger.id }\n        })\n        await tx.purchaseLedger.delete({\n          where: { id: oldPurchaseLedger.id }\n        })\n      }\n\n      // First, restore old stock\n      for (const oldItem of existingInvoice.invoiceItems) {\n        await tx.product.update({\n          where: { id: oldItem.productId },\n          data: { currentStock: { increment: oldItem.qty } }\n        })\n      }\n",
          "StartLine": 1147,
          "TargetContent": "    const result = await prisma.$transaction(async (tx) => {\n      // First, restore old stock\n      for (const oldItem of existingInvoice.invoiceItems) {\n        await tx.product.update({\n          where: { id: oldItem.productId },\n          data: { currentStock: { increment: oldItem.qty } }\n        })\n      }\n"
        },
        {
          "AllowMultiple": false,
          "EndLine": 1263,
          "ReplacementContent": "      // Update distributor financials with new grand total\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      // --- AUTO-GENERATE NEW DISTRIBUTOR PURCHASE ---\n      // 1. Create PurchaseLedger\n      const purchaseLedger = await tx.purchaseLedger.create({\n        data: {\n          supplierName: csaName,\n          invoiceNo: existingInvoice.invoiceNo,\n          date: new Date(),\n          totalAmount: grandTotal,\n          distributorId: existingInvoice.distributorId,\n          csaId: csaId\n        }\n      })\n\n      // 2. Loop through each created invoice item to update/create distributor products and create purchase items\n      for (let i = 0; i < invoiceItemsData.length; i++) {\n        const item = invoiceItemsData[i]\n        const csaProduct = productsData[i].product\n        \n        const rateWithMargin = item.total / item.qty\n\n        let distProduct = await tx.product.findFirst({\n          where: {\n            distributorId: existingInvoice.distributorId,\n            sku: csaProduct.sku\n          }\n        })\n        if (!distProduct || !csaProduct.sku) {\n          distProduct = await tx.product.findFirst({\n            where: {\n              distributorId: existingInvoice.distributorId,\n              name: csaProduct.name\n            }\n          })\n        }\n\n        if (distProduct) {\n          distProduct = await tx.product.update({\n            where: { id: distProduct.id },\n            data: {\n              currentStock: { increment: item.qty },\n              costPrice: rateWithMargin\n            }\n          })\n        } else {\n          distProduct = await tx.product.create({\n            data: {\n              distributorId: existingInvoice.distributorId,\n              name: csaProduct.name,\n              sku: csaProduct.sku || `SKU-${Date.now()}-${i}`,\n              hsn: csaProduct.hsn,\n              batchNo: csaProduct.batchNo,\n              expiryDate: csaProduct.expiryDate,\n              costPrice: rateWithMargin,\n              baseSellingPrice: csaProduct.baseSellingPrice,\n              gstPercentage: csaProduct.gstPercentage,\n              currentStock: item.qty\n            }\n          })\n        }\n\n        await tx.purchaseItem.create({\n          data: {\n            purchaseId: purchaseLedger.id,\n            productId: distProduct.id,\n            distributorId: existingInvoice.distributorId,\n            csaId: csaId,\n            qty: item.qty,\n            costPrice: rateWithMargin,\n            rate: rateWithMargin,\n            gstPercentage: item.gstPercentage,\n            total: item.total,\n            mrp: distProduct.baseSellingPrice,\n            batchNo: csaProduct.batchNo,\n            expiryDate: csaProduct.expiryDate\n          }\n        })\n      }\n\n      return invoice\n    })\n",
          "StartLine": 1253,
          "TargetContent": "      // Update distributor financials with new grand total\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      return invoice\n    })\n"
        },
        {
          "AllowMultiple": false,
          "EndLine": 1308,
          "ReplacementContent": "    const result = await prisma.$transaction(async (tx) => {\n      // Revert distributor stock and delete matching PurchaseLedger\n      const purchaseLedger = await tx.purchaseLedger.findFirst({\n        where: {\n          invoiceNo: existingInvoice.invoiceNo,\n          distributorId: existingInvoice.distributorId\n        },\n        include: { purchaseItems: true }\n      })\n      if (purchaseLedger) {\n        for (const pItem of purchaseLedger.purchaseItems) {\n          await tx.product.update({\n            where: { id: pItem.productId },\n            data: { currentStock: { decrement: pItem.qty } }\n          })\n        }\n        await tx.purchaseItem.deleteMany({\n          where: { purchaseId: purchaseLedger.id }\n        })\n        await tx.purchaseLedger.delete({\n          where: { id: purchaseLedger.id }\n        })\n      }\n\n      // Restore stock\n      for (const item of existingInvoice.invoiceItems) {\n        await tx.product.update({\n          where: { id: item.productId },\n          data: { currentStock: { increment: item.qty } }\n        })\n      }\n\n      // Update distributor financials\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { decrement: getNum(existingInvoice.grandTotal) },\n          pendingCompanyBalance: { increment: getNum(existingInvoice.grandTotal) }\n        }\n      })\n",
          "StartLine": 1291,
          "TargetContent": "    const result = await prisma.$transaction(async (tx) => {\n      // Restore stock\n      for (const item of existingInvoice.invoiceItems) {\n        await tx.product.update({\n          where: { id: item.productId },\n          data: { currentStock: { increment: item.qty } }\n        })\n      }\n\n      // Update distributor financials\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { decrement: getNum(existingInvoice.grandTotal) },\n          pendingCompanyBalance: { increment: getNum(existingInvoice.grandTotal) }\n        }\n      })\n"
        }
      ],
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Updating csa.js to sync distributor purchases",
      "toolSummary": "Modify csa.js file"
    }
  },
  {
    "step_index": 616,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Automatically generate PurchaseLedger and PurchaseItems, and increment distributor's inventory stock when CSA creates a direct invoice for a distributor.",
      "EndLine": 1081,
      "Instruction": "Modify invoice creation transaction to auto-generate corresponding purchase ledger and items for direct-to-distributor invoices.",
      "ReplacementContent": "      const invoice = await tx.invoice.create({\n        data: {\n          invoiceNo: nextInvoiceNo,\n          partyId,\n          distributorId,\n          csaId,\n          createdById: csaId,\n          date: new Date(),\n          taxableValue: totalTaxable,\n          cgst: totalCGST,\n          sgst: totalSGST,\n          igst: totalIGST,\n          grandTotal,\n          invoiceItems: {\n            create: invoiceItemsData\n          }\n        },\n        include: { party: true, distributor: true, invoiceItems: { include: { product: true } } }\n      })\n\n      // --- AUTO GENERATE PURCHASE LEDGER IF DIRECT TO DISTRIBUTOR ---\n      if (!partyId) {\n        const csaUser = await tx.user.findUnique({\n          where: { id: csaId },\n          select: { name: true }\n        })\n\n        const purchaseLedger = await tx.purchaseLedger.create({\n          data: {\n            supplierName: csaUser?.name || 'CSA',\n            invoiceNo: nextInvoiceNo,\n            date: new Date(),\n            totalAmount: grandTotal,\n            distributorId: distributorId,\n            csaId: csaId\n          }\n        })\n\n        // Sync items to distributor's inventory and create PurchaseItems\n        for (let i = 0; i < productsData.length; i++) {\n          const itemData = invoiceItemsData[i]\n          const csaProduct = productsData[i].product\n\n          // Find distributor product by name\n          let distProduct = await tx.product.findFirst({\n            where: {\n              distributorId: distributorId,\n              name: csaProduct.name\n            }\n          })\n\n          if (!distProduct) {\n            distProduct = await tx.product.create({\n              data: {\n                name: csaProduct.name,\n                sku: csaProduct.sku || `SKU-${Date.now()}-${Math.random().toString(36).substr(2, 5)}`,\n                hsn: csaProduct.hsn || '',\n                costPrice: itemData.rate,\n                baseSellingPrice: csaProduct.baseSellingPrice || itemData.rate * 1.2,\n                gstPercentage: csaProduct.gstPercentage,\n                currentStock: itemData.qty,\n                distributorId: distributorId\n              }\n            })\n          } else {\n            distProduct = await tx.product.update({\n              where: { id: distProduct.id },\n              data: {\n                currentStock: { increment: itemData.qty },\n                costPrice: itemData.rate\n              }\n            })\n          }\n\n          // Create purchase item\n          await tx.purchaseItem.create({\n            data: {\n              purchaseId: purchaseLedger.id,\n              productId: distProduct.id,\n              sortOrder: i,\n              qty: itemData.qty,\n              costPrice: itemData.rate,\n              rate: itemData.rate,\n              gstPercentage: itemData.gstPercentage,\n              total: itemData.total,\n              distributorId: distributorId\n            }\n          })\n        }\n\n        // Update distributor financials for purchase debits\n        await tx.distributor.update({\n          where: { id: distributorId },\n          data: {\n            totalCompanyDebits: { increment: grandTotal },\n            pendingCompanyBalance: { increment: grandTotal }\n          }\n        })\n      }\n\n      await tx.distributor.update({\n        where: { id: distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      return {\n        ...invoice,\n        invoiceStockCost,\n        invoiceProfit,\n        profitStatus\n      }",
      "StartLine": 1047,
      "TargetContent": "      const invoice = await tx.invoice.create({\n        data: {\n          invoiceNo: nextInvoiceNo,\n          partyId,\n          distributorId,\n          csaId,\n          createdById: csaId,\n          date: new Date(),\n          taxableValue: totalTaxable,\n          cgst: totalCGST,\n          sgst: totalSGST,\n          igst: totalIGST,\n          grandTotal,\n          invoiceItems: {\n            create: invoiceItemsData\n          }\n        },\n        include: { party: true, distributor: true, invoiceItems: { include: { product: true } } }\n      })\n\n      await tx.distributor.update({\n        where: { id: distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      return {\n        ...invoice,\n        invoiceStockCost,\n        invoiceProfit,\n        profitStatus\n      }",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Modifying csa.js invoice create handler",
      "toolSummary": "Edit csa.js route handler"
    }
  },
  {
    "step_index": 618,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Automatically synchronize distributor's PurchaseLedger, PurchaseItems, and inventory stock when CSA updates a direct invoice.",
      "EndLine": 1263,
      "Instruction": "Modify invoice edit PUT transaction to sync corresponding purchase ledger, items, and inventory for direct invoices.",
      "ReplacementContent": "      // Update distributor financials with new grand total\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      // --- AUTO SYNC PURCHASE LEDGER IF DIRECT TO DISTRIBUTOR ---\n      if (existingInvoice.partyId === null) {\n        const existingPurchase = await tx.purchaseLedger.findFirst({\n          where: {\n            invoiceNo: existingInvoice.invoiceNo,\n            distributorId: existingInvoice.distributorId\n          },\n          include: { purchaseItems: true }\n        })\n\n        if (existingPurchase) {\n          // Revert distributor stock from old purchase items\n          for (const oldPurchItem of existingPurchase.purchaseItems) {\n            await tx.product.update({\n              where: { id: oldPurchItem.productId },\n              data: { currentStock: { decrement: oldPurchItem.qty } }\n            })\n          }\n\n          // Subtract old grand total from distributor totalCompanyDebits and pendingCompanyBalance\n          await tx.distributor.update({\n            where: { id: existingInvoice.distributorId },\n            data: {\n              totalCompanyDebits: { decrement: getNum(existingInvoice.grandTotal) },\n              pendingCompanyBalance: { decrement: getNum(existingInvoice.grandTotal) }\n            }\n          })\n\n          // Delete old purchase items\n          await tx.purchaseItem.deleteMany({\n            where: { purchaseId: existingPurchase.id }\n          })\n\n          // Update purchase ledger total amount\n          await tx.purchaseLedger.update({\n            where: { id: existingPurchase.id },\n            data: {\n              totalAmount: grandTotal\n            }\n          })\n\n          // Create new purchase items and update distributor stock\n          for (let i = 0; i < productsData.length; i++) {\n            const itemData = invoiceItemsData[i]\n            const csaProduct = productsData[i].product\n\n            let distProduct = await tx.product.findFirst({\n              where: {\n                distributorId: existingInvoice.distributorId,\n                name: csaProduct.name\n              }\n            })\n\n            if (!distProduct) {\n              distProduct = await tx.product.create({\n                data: {\n                  name: csaProduct.name,\n                  sku: csaProduct.sku || `SKU-${Date.now()}-${Math.random().toString(36).substr(2, 5)}`,\n                  hsn: csaProduct.hsn || '',\n                  costPrice: itemData.rate,\n                  baseSellingPrice: csaProduct.baseSellingPrice || itemData.rate * 1.2,\n                  gstPercentage: csaProduct.gstPercentage,\n                  currentStock: itemData.qty,\n                  distributorId: existingInvoice.distributorId\n                }\n              })\n            } else {\n              distProduct = await tx.product.update({\n                where: { id: distProduct.id },\n                data: {\n                  currentStock: { increment: itemData.qty },\n                  costPrice: itemData.rate\n                }\n              })\n            }\n\n            await tx.purchaseItem.create({\n              data: {\n                purchaseId: existingPurchase.id,\n                productId: distProduct.id,\n                sortOrder: i,\n                qty: itemData.qty,\n                costPrice: itemData.rate,\n                rate: itemData.rate,\n                gstPercentage: itemData.gstPercentage,\n                total: itemData.total,\n                distributorId: existingInvoice.distributorId\n              }\n            })\n          }\n\n          // Add new grand total to distributor totalCompanyDebits and pendingCompanyBalance\n          await tx.distributor.update({\n            where: { id: existingInvoice.distributorId },\n            data: {\n              totalCompanyDebits: { increment: grandTotal },\n              pendingCompanyBalance: { increment: grandTotal }\n            }\n          })\n        }\n      }\n\n      return invoice\n    })",
      "StartLine": 1253,
      "TargetContent": "      // Update distributor financials with new grand total\n      await tx.distributor.update({\n        where: { id: existingInvoice.distributorId },\n        data: {\n          totalAmountRealized: { increment: grandTotal },\n          pendingCompanyBalance: { decrement: grandTotal }\n        }\n      })\n\n      return invoice\n    })",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Modifying csa.js invoice edit handler",
      "toolSummary": "Edit csa.js route handler"
    }
  },
  {
    "step_index": 620,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Automatically delete distributor's corresponding PurchaseLedger, PurchaseItems, and revert distributor stock on direct invoice deletion.",
      "EndLine": 1318,
      "Instruction": "Modify invoice deletion transaction to delete corresponding purchase ledger, items, and revert inventory for direct invoices.",
      "ReplacementContent": "      // --- AUTO DELETE PURCHASE LEDGER IF DIRECT TO DISTRIBUTOR ---\n      if (existingInvoice.partyId === null) {\n        const existingPurchase = await tx.purchaseLedger.findFirst({\n          where: {\n            invoiceNo: existingInvoice.invoiceNo,\n            distributorId: existingInvoice.distributorId\n          },\n          include: { purchaseItems: true }\n        })\n\n        if (existingPurchase) {\n          // Revert distributor stock from old purchase items\n          for (const oldPurchItem of existingPurchase.purchaseItems) {\n            await tx.product.update({\n              where: { id: oldPurchItem.productId },\n              data: { currentStock: { decrement: oldPurchItem.qty } }\n            })\n          }\n\n          // Subtract old grand total from distributor totalCompanyDebits and pendingCompanyBalance\n          await tx.distributor.update({\n            where: { id: existingInvoice.distributorId },\n            data: {\n              totalCompanyDebits: { decrement: getNum(existingInvoice.grandTotal) },\n              pendingCompanyBalance: { decrement: getNum(existingInvoice.grandTotal) }\n            }\n          })\n\n          // Delete purchase items\n          await tx.purchaseItem.deleteMany({\n            where: { purchaseId: existingPurchase.id }\n          })\n\n          // Delete purchase ledger\n          await tx.purchaseLedger.delete({\n            where: { id: existingPurchase.id }\n          })\n        }\n      }\n\n      // Delete invoice items first\n      await tx.invoiceItem.deleteMany({\n        where: { invoiceId: id }\n      })\n\n      // Now delete the invoice\n      await tx.invoice.delete({\n        where: { id }\n      })\n    })",
      "StartLine": 1308,
      "TargetContent": "      // Delete invoice items first\n      await tx.invoiceItem.deleteMany({\n        where: { invoiceId: id }\n      })\n\n      // Now delete the invoice\n      await tx.invoice.delete({\n        where: { id }\n      })\n    })",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Modifying csa.js invoice delete handler",
      "toolSummary": "Edit csa.js route handler"
    }
  },
  {
    "step_index": 902,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Enable Gemini OCR and set model to gemini-2.5-flash in backend csa.js route.",
      "EndLine": 3600,
      "Instruction": "Remove hardcoded skip and update model to gemini-2.5-flash.",
      "ReplacementContent": "        const response = await ai.models.generateContent({\n          model: 'gemini-2.5-flash',\n          contents: [",
      "StartLine": 3593,
      "TargetContent": "        \n        console.log('🤖 Skipping Gemini OCR, forcing fallback to manual parser');\n        throw new Error('Skip Gemini, use manual parser');\n        \n        const response = await ai.models.generateContent({\n          model: 'gemini-1.5-flash',\n          contents: [",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Enabling Gemini 2.5 Flash in csa.js route",
      "toolSummary": "csa.js model update"
    }
  },
  {
    "step_index": 919,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Clean up markdown code blocks from Gemini response before parsing JSON, and update SDK configuration syntax to use config instead of generationConfig in csa.js.",
      "EndLine": 3619,
      "Instruction": "Clean markdown formatting and use config object for SDK call in csa.js.",
      "ReplacementContent": "        const response = await ai.models.generateContent({\n          model: 'gemini-2.5-flash',\n          contents: [\n            {\n              inlineData: {\n                mimeType,\n                data: dataBuffer.toString(\"base64\")\n              }\n            },\n            flexiblePrompt\n          ],\n          config: {\n            responseMimeType: \"application/json\"\n          }\n        });\n\n        const aiText = response.text;\n        console.log('🤖 Gemini full response:', aiText);\n\n        // Robust cleanup for markdown code blocks\n        let cleanText = aiText.trim();\n        if (cleanText.startsWith('```')) {\n          cleanText = cleanText.replace(/^```(?:json)?\\n?/i, '');\n          cleanText = cleanText.replace(/\\n?```$/i, '');\n        }\n        cleanText = cleanText.trim();\n\n        // Parse Gemini's JSON response\n        let parsedProducts = [];\n        try {\n          parsedProducts = JSON.parse(cleanText);",
      "StartLine": 3597,
      "TargetContent": "        const response = await ai.models.generateContent({\n          model: 'gemini-2.5-flash',\n          contents: [\n            {\n              inlineData: {\n                mimeType,\n                data: dataBuffer.toString(\"base64\")\n              }\n            },\n            flexiblePrompt\n          ],\n          generationConfig: {\n            responseMimeType: \"application/json\"\n          }\n        });\n\n        const aiText = response.text;\n        console.log('🤖 Gemini full response:', aiText);\n        \n        // Parse Gemini's JSON response\n        let parsedProducts = [];\n        try {\n          parsedProducts = JSON.parse(aiText);",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Adding robust JSON cleaning and SDK config updates in csa.js",
      "toolSummary": "csa.js response cleaning"
    }
  },
  {
    "step_index": 1145,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Update Gemini OCR extraction prompt and mapping logic to properly extract GST %, Taxable Value, and compute correct GST-exclusive rates in csa.js.",
      "EndLine": 3740,
      "Instruction": "Rewrite the OCR section to extract taxable_value and gst_pct, and calculate GST-exclusive rates before saving.",
      "ReplacementContent": "        // --- Absolute Precision Invoice Data Extractor ---\n        const flexiblePrompt = `\n          You are an absolute precision Invoice Data Extractor. Extract tabular data from this invoice with perfect accuracy.\n          \n          CRITICAL DATA EXTRACTION RULE: NO INTERNAL MATH OR COMPUTATION!\n          - Never calculate, reverse-engineer, or verify column fields (MRP, Rate, Total) using mathematical operations or formulas.\n          - You must treat every column as a separate visual entity.\n          - If the 'MRP' column has \"329\" and \"(64.41% OFF)\", extract ONLY \"329\" as MRP. Do NOT apply any percentage logic. Do NOT derive MRP from the 'Rate' or 'Total' columns.\n          - Extract 'Rate' exactly as printed (e.g., 64.50, 117.1). Do not modify decimals based on Total or Tax distribution.\n          - Maintain a strict string-to-float casting without dynamic recalculation loops.\n          \n          Strictly follow these mathematical anchors and extraction rules:\n          \n          1. MRP EXTRACTION CRITICAL RULE:\n             - Extract ONLY the primary, top-most numeric value listed in the 'MRP' column.\n             - COMPLETELY IGNORE any text or values inside brackets directly under or next to the MRP (e.g., if the cell contains \"329 \\\\n (64.41% OFF)\", your extracted value for MRP must be EXACTLY 329). Do NOT calculate or subtract anything.\n          \n          2. RATE EXTRACTION CRITICAL RULE:\n             - The 'Rate' column is explicitly printed on the invoice (e.g., 64.50, 117.1, 81.15).\n             - Extract the exact numeric characters present under the 'Rate' header. Do NOT try to re-calculate it or mix it with the discount text.\n          \n          3. TAXABLE VALUE EXTRACTION RULE:\n             - Look for a column labeled 'Taxable', 'Taxable Value', or 'Taxable Amt'.\n             - If found, extract the exact numeric characters present under that header (e.g., 54.66).\n             \n          4. GST PERCENTAGE EXTRACTION RULE:\n             - Look for a column labeled 'GST %', 'GST', 'Tax %', or 'Tax Rate'.\n             - If found, extract the exact numeric percentage value (e.g., 18 or 18%). Remove the '%' symbol.\n          \n          5. TOTAL COLUMN MATH GUARD:\n             - The 'Total' field must be extracted exactly as printed on the invoice text layout. Remove commas.\n          \n          6. BOUNDARY ANCHORING FOR THE TABLE:\n             - The product table strictly begins AFTER the headers \"No\", \"Items\", \"HSN No.\", \"Qty.\", \"MRP\", \"Rate\", \"Disc.\", \"Tax\", \"Total\", \"Product\", \"Taxable\", \"GST %\" or similar headers.\n             - Completely IGNORE all text fields from \"POPPIK LIFESTYLE PVT LTD\", \"Bill To\", \"Ship To\", \"Invoice No\", and \"Dates\" when processing the row items array. Never inject vendor/client addresses or massive amounts into product lines.\n          \n          7. PRODUCT NAME CLEANING:\n             - Strip out any leading Serial Numbers/Row numbers from the product text. For example, if the text is \"15 Poppik Mattepout...\", extract ONLY \"Poppik Mattepout...\".\n             - Do not include raw numerical strings or prefixes that belong to the \"No\" column inside the \"product_name\" field.\n          \n          8. STRICT COLUMN SEPARATION (No Concatenation):\n             - HSN No. is a standard static 8-digit numeric code (e.g., \"33041000\").\n             - Qty is a separate field containing small integers followed by \"PCS\" (e.g., \"18 PCS\", \"5 PCS\").\n             - CRITICAL: Never append or merge the Qty integer to the HSN string. They must be extracted into completely separate keys: \"hsn_no\": \"33041000\" and \"quantity\": 18.\n \n          9. DISCOUNT EXTRACTION CRITICAL RULE:\n             - The \"Disc.\" is a dedicated column on the invoice - extract ONLY from that column!\n             - DO NOT extract the percentage OFF value from the MRP column's brackets (like \"64.41% OFF\") as discount - that is NOT the discount value!\n             - The \"Disc.\" column has the actual discount amount (e.g., 63.23, 17.56, etc.)\n          \n          Output strictly as a valid JSON array matching this data type format:\n          [\n            {\n              \"row_no\": 1,\n              \"product_name\": \"String\",\n              \"hsn_no\": \"String\",\n              \"quantity\": Integer,\n              \"mrp\": Float,\n              \"rate\": Float,\n              \"discount\": Float,\n              \"taxable_value\": Float,\n              \"gst_pct\": Float,\n              \"total\": Float\n            }\n          ]\n          Do not output any markdown text or conversational greetings outside of the JSON block.\n        `;\n        const response = await ai.models.generateContent({\n          model: 'gemini-2.5-flash',\n          contents: [\n            {\n              inlineData: {\n                mimeType,\n                data: dataBuffer.toString(\"base64\")\n              }\n            },\n            flexiblePrompt\n          ],\n          config: {\n            responseMimeType: \"application/json\"\n          }\n        });\n\n        const aiText = response.text;\n        console.log('🤖 Gemini full response:', aiText);\n\n        // Robust cleanup for markdown code blocks\n        let cleanText = aiText.trim();\n        if (cleanText.startsWith('```')) {\n          cleanText = cleanText.replace(/^```(?:json)?\\n?/i, '');\n          cleanText = cleanText.replace(/\\n?```$/i, '');\n        }\n        cleanText = cleanText.trim();\n\n        // Parse Gemini's JSON response\n        let parsedProducts = [];\n        try {\n          parsedProducts = JSON.parse(cleanText);\n          if (!Array.isArray(parsedProducts)) {\n            console.warn('Gemini returned non-array, falling back to text-based parsing');\n            throw new Error('Non-array response');\n          }\n        } catch (parseErr) {\n          console.error('Error parsing Gemini JSON:', parseErr);\n          throw parseErr;\n        }\n\n        // --- VALIDATE TOTAL VALUES AND MAP SCHEMA ---\n        parsedProducts = parsedProducts.map((product, index) => {\n          // 1. Clean Product Name (Serial Number and accidental prefixes fix)\n          let cleanName = String(product.product_name || product.productName || '').trim();\n          // Clean text like \"15Poppik\" or \"45Poppik\" to \"Poppik\"\n          cleanName = cleanName.replace(/^\\d+([Pp]oppik)/, '$1');\n          // Also remove leading numbers followed by space\n          cleanName = cleanName.replace(/^\\d+\\s*/, '');\n          \n          // Map new AI schema keys to our existing ones\n          const hsn = product.hsn_no || product.hsn;\n          const qtyVal = product.quantity || product.qty;\n          const gstPercentage = parseFloat(product.gst_pct || product.tax_pct || product.tax || 18);\n          const discVal = product.discount || product.disc || product.discount_pct;\n\n          // Calculate correct GST-exclusive cost price / rate!\n          const qty = parseInt(qtyVal, 10) || 1;\n          const rateVal = parseFloat(product.rate) || 0;\n          const totalVal = parseFloat(product.total) || 0;\n          const taxableVal = parseFloat(product.taxable_value || product.taxableValue || 0);\n\n          let costPriceVal = rateVal; // Fallback\n          if (taxableVal && taxableVal > 0) {\n            costPriceVal = taxableVal / qty;\n          } else {\n            // Check if rate is GST-inclusive\n            const expectedTotalIfRateExcl = rateVal * qty * (1 + gstPercentage / 100);\n            const expectedTotalIfRateIncl = rateVal * qty;\n            if (Math.abs(expectedTotalIfRateIncl - totalVal) < 1.0) {\n              costPriceVal = rateVal / (1 + gstPercentage / 100);\n            } else if (Math.abs(expectedTotalIfRateExcl - totalVal) < 1.0) {\n              costPriceVal = rateVal;\n            }\n          }\n          \n          // 2. Strict Quantity Truncation (If HSN leaks inside Qty)\n          let rawQtyStr = String(qtyVal).replace(/[^0-9]/g, ''); // Extract only digits\n          let cleanQty = parseInt(rawQtyStr, 10) || 1;\n          \n          // Agar galti se HSN full digits (33041000) ke sath qty string aa gayi h\n          if (rawQtyStr.startsWith(\"33041000\") && rawQtyStr.length > 8) {\n            cleanQty = parseInt(rawQtyStr.substring(8), 10) || 1;\n          } else if (cleanQty > 10000) {\n            // Agar number abhi bhi bound se bahar h toh fall back to default row pack\n            cleanQty = 1;\n          } else if (cleanQty > 2147483647) { \n            console.log(`[Warning] Quantity ${cleanQty} is too large for INT4, resetting to standard loop default.`); \n            cleanQty = 12; // Fallback\n          }\n          \n          // 3. Use cleanInvoiceRow for MRP/Rate/Total/Discount - NO INTERNAL MATH/RECALCULATION!\n          const cleanedValues = cleanInvoiceRow(product);\n\n          // Common tax percentages - we should NEVER treat these as discount!\n          const commonTaxPercentages = [5, 9, 12, 18, 28];\n          let finalDiscount = discVal || cleanedValues.discount;\n          if (finalDiscount != null && !isNaN(parseFloat(finalDiscount))) {\n            const numVal = parseFloat(finalDiscount);\n            if (commonTaxPercentages.includes(numVal)) {\n              finalDiscount = null;\n            }\n          }\n          return {\n            ...product,\n            productName: cleanName,\n            hsn,\n            qty: cleanQty,\n            mrp: cleanedValues.mrp,\n            rate: costPriceVal, // Store the GST-exclusive rate!\n            total: cleanedValues.total || totalVal,\n            discount: finalDiscount,\n            tax_pct: gstPercentage\n          };\n        });\n        \n        // Extract invoice metadata using text-based extraction for backward compatibility\n        const pdfParse = require('pdf-parse');\n        const rawTextData = await pdfParse(dataBuffer);\n        invoiceMetadata = extractInvoiceMetadata(rawTextData.text);\n        console.log('Extracted invoice metadata:', invoiceMetadata);\n        \n        // --- KEEP EXISTING RESPONSE FORMAT FOR BACKWARD COMPATIBILITY ---\n        items = parsedProducts.map(product => {\n          const productName = product.productName?.trim() || '';\n          const costPrice = product.rate;\n          \n          const mappedItem = {\n            productName,\n            sku: '',\n            hsn: String(product.hsn || '33041000'),\n            batchNo: '',\n            expiryDate: null,\n            costPrice: costPrice, // GST-exclusive cost price\n            gstPercentage: product.tax_pct || 18,\n            quantity: parseInt(product.qty, 10) || 0,\n            rate: costPrice, // GST-exclusive rate\n            discount: product.discount,\n            total: product.total\n          };\n          \n          console.log('🔄 Mapped Gemini item:', {\n            productName,\n            discount: product.discount,\n            rate: costPrice,\n            total: product.total\n          });\n          \n          return mappedItem;\n        });",
      "StartLine": 3535,
      "TargetContent": "        // --- Absolute Precision Invoice Data Extractor ---\n        const flexiblePrompt = `\n          You are an absolute precision Invoice Data Extractor. Extract tabular data from this invoice with perfect accuracy.\n          \n          CRITICAL DATA EXTRACTION RULE: NO INTERNAL MATH OR COMPUTATION!\n          - Never calculate, reverse-engineer, or verify column fields (MRP, Rate, Total) using mathematical operations or formulas.\n          - You must treat every column as a separate visual entity.\n          - If the 'MRP' column has \"329\" and \"(64.41% OFF)\", extract ONLY \"329\" as MRP. Do NOT apply any percentage logic. Do NOT derive MRP from the 'Rate' or 'Total' columns.\n          - Extract 'Rate' exactly as printed (e.g., 117.1). Do not modify decimals based on Total or Tax distribution.\n          - Maintain a strict string-to-float casting without dynamic recalculation loops.\n          \n          Strictly follow these mathematical anchors and extraction rules:\n          \n          1. MRP EXTRACTION CRITICAL RULE:\n             - Extract ONLY the primary, top-most numeric value listed in the 'MRP' column.\n             - COMPLETELY IGNORE any text or values inside brackets directly under or next to the MRP (e.g., if the cell contains \"329 \\\\n (64.41% OFF)\", your extracted value for MRP must be EXACTLY 329). Do NOT calculate or subtract anything.\n          \n          2. RATE EXTRACTION CRITICAL RULE:\n             - The 'Rate' column is explicitly printed on the invoice (e.g., 117.1, 81.15, 98.2).\n             - Extract the exact numeric characters present under the 'Rate' header. Do NOT try to re-calculate it or mix it with the discount text.\n          \n          3. TOTAL COLUMN MATH GUARD:\n             - The 'Total' field must be extracted exactly as printed on the invoice text layout (e.g., 2,412.59 should be parsed as 2412.59). Remove commas before saving.\n          \n          4. BOUNDARY ANCHORING FOR THE TABLE:\n             - The product table strictly begins AFTER the headers \"No\", \"Items\", \"HSN No.\", \"Qty.\", \"MRP\", \"Rate\", \"Disc.\", \"Tax\", \"Total\".\n             - Completely IGNORE all text fields from \"POPPIK LIFESTYLE PVT LTD\", \"Bill To\", \"Ship To\", \"Invoice No\", and \"Dates\" when processing the row items array. Never inject vendor/client addresses or massive amounts into product lines.\n          \n          5. PRODUCT NAME CLEANING:\n             - Strip out any leading Serial Numbers/Row numbers from the product text. For example, if the text is \"15 Poppik Mattepout...\", extract ONLY \"Poppik Mattepout...\".\n             - Do not include raw numerical strings or prefixes that belong to the \"No\" column inside the \"product_name\" field.\n          \n          6. STRICT COLUMN SEPARATION (No Concatenation):\n             - HSN No. is a standard static 8-digit numeric code (e.g., \"33041000\").\n             - Qty is a separate field containing small integers followed by \"PCS\" (e.g., \"18 PCS\", \"5 PCS\").\n             - CRITICAL: Never append or merge the Qty integer to the HSN string. They must be extracted into completely separate keys: \"hsn_no\": \"33041000\" and \"quantity\": 18.\n \n          7. DISCOUNT EXTRACTION CRITICAL RULE:\n             - The \"Disc.\" is a dedicated column on the invoice - extract ONLY from that column!\n             - DO NOT extract the percentage OFF value from the MRP column's brackets (like \"64.41% OFF\") as discount - that is NOT the discount value!\n             - The \"Disc.\" column has the actual discount amount (e.g., 63.23, 17.56, etc.)\n          \n          Output strictly as a valid JSON array matching this data type format:\n          [\n            {\n              \"row_no\": 1,\n              \"product_name\": \"String\",\n              \"hsn_no\": \"String\",\n              \"quantity\": Integer,\n              \"mrp\": Float,\n              \"rate\": Float,\n              \"disc\": Float,\n              \"tax\": Float,\n              \"total\": Float\n            }\n          ]\n          Do not output any markdown text or conversational greetings outside of the JSON block.\n        `;\n        const response = await ai.models.generateContent({\n          model: 'gemini-2.5-flash',\n          contents: [\n            {\n              inlineData: {\n                mimeType,\n                data: dataBuffer.toString(\"base64\")\n              }\n            },\n            flexiblePrompt\n          ],\n          config: {\n            responseMimeType: \"application/json\"\n          }\n        });\n \n        const aiText = response.text;\n        console.log('🤖 Gemini full response:', aiText);\n \n        // Robust cleanup for markdown code blocks\n        let cleanText = aiText.trim();\n        if (cleanText.startsWith('```')) {\n          cleanText = cleanText.replace(/^```(?:json)?\\n?/i, '');\n          cleanText = cleanText.replace(/\\n?```$/i, '');\n        }\n        cleanText = cleanText.trim();\n \n        // Parse Gemini's JSON response\n        let parsedProducts = [];\n        try {\n          parsedProducts = JSON.parse(cleanText);\n          if (!Array.isArray(parsedProducts)) {\n            console.warn('Gemini returned non-array, falling back to text-based parsing');\n            throw new Error('Non-array response');\n          }\n        } catch (parseErr) {\n          console.error('Error parsing Gemini JSON:', parseErr);\n          throw parseErr;\n        }\n \n        // --- VALIDATE TOTAL VALUES AND MAP SCHEMA ---\n        parsedProducts = parsedProducts.map((product, index) => {\n          // 1. Clean Product Name (Serial Number and accidental prefixes fix)\n          let cleanName = String(product.product_name || product.productName || '').trim();\n          // Clean text like \"15Poppik\" or \"45Poppik\" to \"Poppik\"\n          cleanName = cleanName.replace(/^\\d+([Pp]oppik)/, '$1');\n          // Also remove leading numbers followed by space\n          cleanName = cleanName.replace(/^\\d+\\s*/, '');\n          \n          // Map new AI schema keys to our existing ones\n          const hsn = product.hsn_no || product.hsn;\n          const qtyVal = product.quantity || product.qty;\n          const taxPct = product.tax_pct || product.tax || 18;\n          const discVal = product.disc || product.discount || product.discount_pct;\n          \n          // 2. Strict Quantity Truncation (If HSN leaks inside Qty)\n          let rawQtyStr = String(qtyVal).replace(/[^0-9]/g, ''); // Extract only digits\n          let cleanQty = parseInt(rawQtyStr, 10) || 1;\n          \n          // Agar galti se HSN full digits (33041000) ke sath qty string aa gayi h\n          if (rawQtyStr.startsWith(\"33041000\") && rawQtyStr.length > 8) {\n            cleanQty = parseInt(rawQtyStr.substring(8), 10) || 1;\n          } else if (cleanQty > 10000) {\n            // Agar number abhi bhi bound se bahar h toh fall back to default row pack\n            cleanQty = 1;\n          } else if (cleanQty > 2147483647) { \n            console.log(`[Warning] Quantity ${cleanQty} is too large for INT4, resetting to standard loop default.`); \n            cleanQty = 12; // Agar barcode galti se quantity mein aa gaya hai toh use standard pack (12/1) par fallback karein \n          }\n          \n          // 3. Use cleanInvoiceRow for MRP/Rate/Total/Discount - NO INTERNAL MATH/RECALCULATION!\n          const cleanedValues = cleanInvoiceRow(product);\n \n          // Common tax percentages - we should NEVER treat these as discount!\n          const commonTaxPercentages = [5, 9, 12, 18, 28];\n          let finalDiscount = discVal || cleanedValues.discount;\n          if (finalDiscount != null && !isNaN(parseFloat(finalDiscount))) {\n            const numVal = parseFloat(finalDiscount);\n            if (commonTaxPercentages.includes(numVal)) {\n              finalDiscount = null;\n            }\n          }\n          return {\n            ...product,\n            productName: cleanName,\n            hsn,\n            qty: cleanQty,\n            mrp: cleanedValues.mrp,\n            rate: (product.rate != null && !isNaN(parseFloat(product.rate))) ? parseFloat(product.rate) : cleanedValues.rate,\n            total: cleanedValues.total,\n            discount: finalDiscount,\n            tax_pct: taxPct\n          };\n        });\n        \n        // Extract invoice metadata using text-based extraction for backward compatibility\n        const pdfParse = require('pdf-parse');\n        const rawTextData = await pdfParse(dataBuffer);\n        invoiceMetadata = extractInvoiceMetadata(rawTextData.text);\n        console.log('Extracted invoice metadata:', invoiceMetadata);\n        \n        // --- KEEP EXISTING RESPONSE FORMAT FOR BACKWARD COMPATIBILITY ---\n        items = parsedProducts.map(product => {\n          const productName = product.productName?.trim() || '';\n          let costPrice = parseFloat(product.rate) || 0;\n          \n          // Apply product-specific defaults\n          if (productName.includes(\"Liquid Matte Lipstick\")) {\n            if (!costPrice || costPrice > 200) {\n              costPrice = 117.10;\n            }\n          } else if (productName.includes(\"Mattepout Bullet Lipstick\")) {\n            if (!costPrice || costPrice > 200) {\n              costPrice = costPrice || 81.15;\n            }\n          } else if (productName.includes(\"Boldeyes Intense Smudge-Proof Kajal\")) {\n            if (!costPrice || costPrice > 200) {\n              costPrice = 117.10;\n            }\n          }\n          \n          const mappedItem = {\n            productName,\n            sku: '',\n            hsn: String(product.hsn || '33041000'),\n            batchNo: '',\n            expiryDate: null,\n            costPrice: product.rate || costPrice,\n            gstPercentage: product.tax_pct || (product.tax ? parseFloat(product.tax.replace(/[^0-9.]/g, '')) : 18),\n            quantity: parseInt(product.qty, 10) || 0,\n            rate: product.rate,\n            discount: product.discount,\n            total: product.total\n          };",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Updating Gemini OCR block in csa.js",
      "toolSummary": "Update csa.js OCR parser"
    }
  },
  {
    "step_index": 1149,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Correct syntax issue on mimeType line in csa.js",
      "EndLine": 3535,
      "Instruction": "Restore correct mimeType assignment and separate the comment.",
      "ReplacementContent": "        const mimeType = isPdf ? \"application/pdf\" : req.file.mimetype;\n        \n        // --- Absolute Precision Invoice Data Extractor ---",
      "StartLine": 3533,
      "TargetContent": "        const mimeType = isPdf ? \"app        // --- Absolute Precision Invoice Data Extractor ---\n        const flexiblePrompt = `",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Fixing mimeType syntax error in csa.js",
      "toolSummary": "Fix csa.js syntax error"
    }
  },
  {
    "step_index": 1153,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Restore flexiblePrompt variable definition in csa.js",
      "EndLine": 3537,
      "Instruction": "Add const flexiblePrompt = ` statement back.",
      "ReplacementContent": "        // --- Absolute Precision Invoice Data Extractor ---\n        const flexiblePrompt = `\n          You are an absolute precision Invoice Data Extractor. Extract tabular data from this invoice with perfect accuracy.",
      "StartLine": 3535,
      "TargetContent": "        // --- Absolute Precision Invoice Data Extractor ---\n          You are an absolute precision Invoice Data Extractor. Extract tabular data from this invoice with perfect accuracy.",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Restoring flexiblePrompt declaration",
      "toolSummary": "Restore flexiblePrompt in csa.js"
    }
  },
  {
    "step_index": 1166,
    "name": "replace_file_content",
    "args": {
      "AllowMultiple": false,
      "Description": "Remove duplicate block and syntax error in csa.js mapped items block.",
      "EndLine": 3763,
      "Instruction": "Remove duplicate mappedItem return block and trailing comma.",
      "ReplacementContent": "          \n          return mappedItem;\n        });",
      "StartLine": 3748,
      "TargetContent": "          \n          return mappedItem;\n        });,\n            total: product.total\n          };\n          \n          console.log('🔄 Mapped Gemini item:', {\n            productName,\n            discount: product.discount,\n            rate: product.rate,\n            total: product.total\n          });\n          \n          return mappedItem;\n        });",
      "TargetFile": "c:\\Users\\admin\\Desktop\\billing_inventory\\backend\\src\\routes\\csa.js",
      "toolAction": "Cleaning up duplicate block in csa.js",
      "toolSummary": "Clean up csa.js syntax"
    }
  }
]